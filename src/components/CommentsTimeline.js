import React from 'react';
import CommentAll from '../containers/CommentAll';

const CommentsTimeline = (props) => {
	return (
		<div>
			<CommentAll />
		</div>
	);
};

export default CommentsTimeline;
